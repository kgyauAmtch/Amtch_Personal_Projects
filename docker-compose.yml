# version: "3.8" # Specify the version of Docker Compose to use

services:

  zookeeper:
    image: bitnami/zookeeper:latest
    container_name: zookeeper
    ports:
      - "2181:2181"
    environment:
      - ALLOW_ANONYMOUS_LOGIN=yes
    networks:
      - real_data_network

  kafka:
    image: bitnami/kafka:3.6.1-debian-11-r0  
    container_name: kafka
    ports:
      - "9092:9092"
    environment:
      - KAFKA_BROKER_ID=1
      - KAFKA_ZOOKEEPER_CONNECT=zookeeper:2181
      - KAFKA_LISTENERS=PLAINTEXT://:9092
      - KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://kafka:9092
      - KAFKA_CFG_AUTO_CREATE_TOPICS_ENABLE=true
      - ALLOW_PLAINTEXT_LISTENER=yes
      # - KAFKA_ENABLE_KRAFT=no 
    healthcheck:
      test: ["CMD", "kafka-broker-api-versions.sh", "--bootstrap-server", "kafka:9092"]
      # test: ["CMD-SHELL", "/opt/bitnami/kafka/bin/kafka-topics.sh --bootstrap-server kafka:9092 --list || exit 1"]
      interval: 10s
      retries: 5
      timeout: 5s
    depends_on:
      - zookeeper
    networks:
      - real_data_network

  db:
    container_name: heartbeatdata
    image: postgres:15  # Use Postgres version 15
    restart: always  # Ensure the service restarts automatically if it fails
    environment:
      POSTGRES_USER: proj5kafka  # Custom Postgres username
      POSTGRES_PASSWORD: pass_word    # Strong password for Postgres
      POSTGRES_DB: heart_rate_db      # Custom default database
    ports:
      - "5435:5432"  # Map local port 5433 to container's port 5432
    volumes:
      - postgres_data:/var/lib/postgresql/data  # Persistent volume for Postgres data
      - ./db/postgres_setup.sql:/docker-entrypoint-initdb.d/postgres_setup.sql  # Custom init SQL script
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U proj5kafka -d heart_rate_db"]  # Health check to ensure Postgres is ready
      interval: 5s  # Check every 5 seconds
      timeout: 5s   # Timeout after 5 seconds
      retries: 10   # Retry 10 times before marking the service as unhealthy
    networks:
      - real_data_network  # Connect the service to the custom network

  kafka_producer:
    image: python:3.10-slim
    # build: ./consumer  # Path to Dockerfile for the consumer (described below)
    container_name: kafka_producer
    volumes:
      - ./src:/opt/spark/work-dir 
      - ./data:/opt/spark/data    # Mount your source code folder
    working_dir: /opt/spark/work-dir
    environment:
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092  # Kafka service name from Docker Compose
      KAFKA_TOPIC: producer_to_consumer
      POSTGRES_HOST: db
      POSTGRES_USER: proj5kafka
      POSTGRES_PASSWORD: pass_word
      POSTGRES_DB: heart_rate_db
    command: >
      sh -c "pip install kafka-python  && python kafka_producer.py"  # Install kafka-python and run producer
    depends_on:
      - kafka
    networks:
      - real_data_network

 

networks:
   real_data_network:
     driver: bridge  # Use the bridge network driver for communication between containers

volumes:
  postgres_data: 

  # spark: # Build from the current directory
  #   container_name: spark-2  # Add container name for clarity
  #   image: bitnami/spark:latest  # Use the latest Bitnami Spark image

  #   volumes:
  #     - ./src:/opt/spark/work-dir  # Mount the src folder to the working directory inside the container
  #     - ./data:/opt/spark/data   
  #     - ./jars:/opt/spark/jars   
  #   working_dir: /opt/spark/work-dir  # Set working directory for Spark
    
    
  #   command: >
  #     spark-submit 
  #      --packages org.apache.spark:spark-sql-kafka-0-10_2.12:3.5.0 
  #      --jars /opt/spark/jars/kafka-clients-3.4.0.jar,/opt/spark/jars/postgresql-42.7.5.jar,/opt/spark/jars/spark-sql-kafka-0-10_2.13-3.5.5.jar 
  #      /opt/spark/work-dir/kafka_producer.py

  #   ports:
  #     - "4041:4040"  # Map local port 4041 to Spark UI port 4040
  #   depends_on:
  #     db:
  #       condition: service_healthy  # Wait until the Postgres service is healthy
  #   networks:
  #     - real_data_network  # Connect the service to the custom network

 # Define the volume for Postgres data storage
  



